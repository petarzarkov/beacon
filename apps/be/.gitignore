node_modules
dist
*.log
.env
.env.local

# Build output, generated by `bun run build:fe` and served at runtime.
public/

# The compiled agent release + its manifest, published by `bun run build:agent`.
data/
